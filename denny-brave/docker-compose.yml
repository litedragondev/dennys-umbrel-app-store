version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-brave_web_1
      APP_PORT: 3000

  web:
    image: ghcr.io/linuxserver/brave:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/config:rw
    environment:
     CUSTOM_USER: admin
     PASSWORD: umbrel
