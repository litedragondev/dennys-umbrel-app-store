version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-websurfx_web_1
      APP_PORT: 8080

  web:
    image: neonmmd/websurfx:1.0.0-memory@sha256:42385c5c3c6592bb38962b6cd224fc367f598fa231ee083c2aef5472ae275b8e
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data/websurfx/:/etc/xdg/websurfx/:rw
