version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-websurfx_web_1
      APP_PORT: 8080

  web:
    image: websurfx:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data/websurfx/:/etc/xdg/websurfx/:rw