events {}

http {
    server {
        listen 80;

        location /assets/media/ {
            alias /usr/share/nginx/html/assets/media/;
            autoindex on;
        }

        # nginx dient hier **nur** als statischer Media-Server,
        # Web UI wird über app_proxy auf 8222 bereitgestellt, daher kein Proxy hier.
    }
}
