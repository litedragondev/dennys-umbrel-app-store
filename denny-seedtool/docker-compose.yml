version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: denny-seedtool_web_1
      APP_PORT: 8921
      
  web:
    image: dennysubke/seedtool:2.2.9
    restart: on-failure
    environment:
      NODE_ENV: production
    volumes:
      - ${APP_DATA_DIR}/data:/data
