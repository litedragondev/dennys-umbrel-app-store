version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-vito_app_1
      APP_PORT: 80

  app:
    image: globusgroup/vito:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/var/www/html/storage