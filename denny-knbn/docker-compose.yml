version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-kanbn_web_1
      APP_PORT: 3000

  web:
    image: ghcr.io/kanbn/kan:latest
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/.env:/app/.env

