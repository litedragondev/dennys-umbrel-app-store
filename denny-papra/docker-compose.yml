version: '3.7'

services:
  app_proxy:
    environment:
      APP_HOST: denny-papra_web_1
      APP_PORT: 1221

  web:
    image: ghcr.io/papra-hq/papra:latest
    restart: on-failure
    #volumes:
      # ${APP_DATA_DIR}/data:/app/app-data:rw
